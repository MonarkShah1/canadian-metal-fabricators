---
import Button from './ui/Button.astro';
---

<section class="py-24 bg-slate-950 text-white relative overflow-hidden border-t border-slate-900">
  
  <div class="container mx-auto px-6 md:px-12 relative z-10">
    
    <!-- Editorial Header -->
    <div class="text-center max-w-4xl mx-auto mb-16 reveal">
      <h2 class="font-montserrat font-bold text-4xl md:text-5xl mb-6 leading-tight">
        The Price of <span class="text-red-500">Uncertainty.</span>
      </h2>
      <p class="font-poppins text-slate-400 text-lg max-w-2xl mx-auto">
        When your offshore broker goes dark, your schedule takes the hit. A 20% saving on the PO vanishes the moment your site falls behind.
      </p>
    </div>

    <!-- The "Slim Timeline" Visualization -->
    <div class="max-w-5xl mx-auto mb-20 relative reveal">
            
        <!-- Context Labels -->
        <div class="flex justify-between text-xs font-mono text-slate-500 mb-4 uppercase tracking-widest px-1">
            <span>Production Start</span>
            <span class="text-red-400">Delay Impact</span>
        </div>

        <!-- The Slim Timeline Bar -->
        <div class="relative w-full flex items-center mb-8">
            
            <!-- 1. The "Good" Part (Standard Lead Time) -->
            <div class="h-1 bg-slate-700 w-2/3 rounded-l-full relative group">
                <div class="absolute -top-6 left-1/2 -translate-x-1/2 opacity-0 group-hover:opacity-100 transition text-[10px] text-slate-400 uppercase tracking-wider">Standard Lead Time</div>
                <div class="absolute right-0 top-1/2 -translate-y-1/2 w-2 h-2 bg-slate-500 rounded-full"></div>
            </div>

            <!-- 2. The "Gap" (Jagged/Red) -->
            <div class="h-1 w-1/3 bg-red-500/30 relative flex items-center">
                 <div class="absolute inset-0 bg-red-500 animate-pulse-slow"></div>
                 <!-- Jagged Break Icon in Center -->
                 <div class="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 bg-slate-950 px-2 text-red-500">
                    <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
                 </div>
            </div>
            
            <!-- End Cap -->
             <div class="w-1 h-3 bg-red-600 rounded-full absolute right-0"></div>
        </div>

        <!-- The Cost Connection -->
        <div class="flex justify-end pr-[5%] relative">
                
                <!-- Dotted Connecting Line -->
                <div class="absolute -top-8 right-[15%] w-px h-12 border-l border-dashed border-red-500/50"></div>

                <!-- Sleek Cost Card -->
                <div class="flex flex-col items-end text-right">
                    <div class="text-[10px] uppercase tracking-widest text-red-400 font-bold mb-1">Projected Cost Impact</div>
                    <div class="text-4xl md:text-5xl font-mono font-black text-white tracking-tighter mb-2">-$15,000<span class="text-lg text-slate-500 font-normal">/day</span></div>
                    <div class="text-xs text-slate-500 flex items-center gap-2">
                        <span class="w-1.5 h-1.5 rounded-full bg-red-500 animate-pulse"></span> Site Overhead + Penalties
                    </div>
                </div>
        </div>

    </div>

    <!-- The Problem Statement -->
    <div class="max-w-3xl mx-auto text-center reveal border-t border-slate-900 pt-16 relative">
        <h3 class="text-2xl font-bold text-white mb-6">
            Stop buying "Hope."
        </h3>
        <p class="text-slate-400 text-lg leading-relaxed mb-12">
            Standard brokers have no control. They are just mailboxes forwarding your drawings to a factory they've never visited. You aren't just losing money—you're risking the Critical Path.
        </p>

        <!-- The Transition Connector -->
        <div class="absolute -bottom-32 left-1/2 -translate-x-1/2 flex flex-col items-center z-30">
            <!-- Vertical Line (Continuous) -->
            <div class="w-px h-32 bg-slate-700"></div>
        </div>
    </div>
    
  </div>
</section>
    
  </div>
</section>

<style>
    @keyframes pulse-slow {
        0%, 100% { opacity: 0.5; }
        50% { opacity: 1; }
    }
    .animate-pulse-slow {
        animation: pulse-slow 2s infinite ease-in-out;
    }
</style>