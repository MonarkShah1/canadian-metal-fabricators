---
import Button from './ui/Button.astro';
---

<section class="py-24 bg-slate-950 text-white relative overflow-hidden border-t border-slate-900">
  
  <div class="container mx-auto px-6 md:px-12 relative z-10">
    
    <!-- Editorial Header -->
    <div class="text-center max-w-4xl mx-auto mb-20 reveal">
      <h2 class="font-montserrat font-bold text-4xl md:text-5xl mb-6 leading-tight">
        Your Global Supply Chain has a <span class="text-red-500">Blind Spot.</span>
      </h2>
      <p class="font-poppins text-slate-400 text-lg max-w-2xl mx-auto">
        Six weeks of radio silence is a liability. Stop managing your supply chain with hope and a tracking number.
      </p>
    </div>

    <!-- The "Fog of War" Visualization -->
    <div class="max-w-5xl mx-auto mb-20 relative reveal">
        
        <!-- The Map Interface -->
        <div class="bg-slate-900 rounded-3xl border border-slate-800 p-2 shadow-2xl relative overflow-hidden group">
            
            <!-- Map Background (Abstract) -->
            <div class="absolute inset-0 opacity-20 pointer-events-none">
                 <svg class="w-full h-full text-slate-700" fill="none" viewBox="0 0 1000 500" preserveAspectRatio="none">
                    <!-- Grid -->
                    <pattern id="grid" width="40" height="40" patternUnits="userSpaceOnUse">
                        <path d="M 40 0 L 0 0 0 40" fill="none" stroke="currentColor" stroke-width="0.5"/>
                    </pattern>
                    <rect width="100%" height="100%" fill="url(#grid)" />
                    
                    <!-- Rough Continents (Abstract) -->
                    <path d="M100,100 Q200,50 300,150 T500,100 T800,150" stroke="currentColor" stroke-width="2" stroke-dasharray="4 4" class="opacity-30"/>
                 </svg>
            </div>

            <!-- The Route UI -->
            <div class="relative bg-slate-950/80 backdrop-blur-sm rounded-2xl p-8 md:p-12 border border-slate-800 h-[400px] flex flex-col justify-center items-center">
                
                <!-- Connection Line -->
                <div class="absolute top-1/2 left-20 right-20 h-1 bg-slate-800 rounded-full -translate-y-1/2"></div>
                
                <!-- Start Point: India -->
                <div class="absolute top-1/2 left-10 md:left-24 -translate-y-1/2 flex flex-col items-center gap-4 z-10">
                    <div class="w-4 h-4 bg-slate-500 rounded-full"></div>
                    <div class="text-xs font-mono text-slate-500 uppercase tracking-widest text-center">
                        Origin<br/>Mumbai
                    </div>
                </div>

                <!-- End Point: Canada -->
                <div class="absolute top-1/2 right-10 md:right-24 -translate-y-1/2 flex flex-col items-center gap-4 z-10">
                    <div class="w-6 h-6 border-2 border-slate-600 rounded-full"></div>
                    <div class="text-xs font-mono text-slate-500 uppercase tracking-widest text-center">
                        Destination<br/>Toronto
                    </div>
                </div>

                <!-- The "Lost" Shipment (Center) -->
                <div class="relative z-20 flex flex-col items-center">
                    <!-- Warning Pulse -->
                    <div class="absolute inset-0 bg-red-500/20 blur-xl rounded-full animate-pulse"></div>
                    
                    <!-- Icon -->
                    <div class="w-16 h-16 bg-slate-900 border-2 border-red-500 rounded-2xl flex items-center justify-center shadow-[0_0_30px_rgba(239,68,68,0.3)] relative">
                        <svg class="w-8 h-8 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18.364 18.364A9 9 0 005.636 5.636m12.728 12.728A9 9 0 015.636 5.636m12.728 12.728L5.636 5.636"></path></svg>
                        
                        <!-- Alert Badge -->
                        <div class="absolute -top-3 -right-3 bg-red-600 text-white text-[10px] font-bold px-2 py-0.5 rounded shadow-lg animate-bounce">
                            !
                        </div>
                    </div>

                    <!-- Status Text -->
                    <div class="mt-6 text-center space-y-1">
                        <div class="text-red-500 font-mono font-bold text-lg tracking-wider">CONNECTION LOST</div>
                        <div class="text-slate-500 text-sm font-mono">Last Update: 14 Days Ago</div>
                        <div class="text-xs text-slate-600 font-mono uppercase bg-slate-900/50 px-2 py-1 rounded inline-block mt-2 border border-slate-800">
                            Location: Unknown
                        </div>
                    </div>
                </div>

            </div>
        </div>

    </div>

    <!-- The Problem Statement -->
    <div class="max-w-4xl mx-auto text-center reveal">
        <h3 class="text-2xl md:text-3xl font-bold text-white mb-6">
            Stop sourcing into the void.
        </h3>
        <p class="text-slate-400 text-lg leading-relaxed mb-10">
            Standard brokers are just mailboxes. They forward your PO to a factory they've never visited and hope for the best. <br class="hidden md:block"/>
            When the signal goes dark, you're the one explaining the delay to your VP.
        </p>

        <!-- Lead-in to Solution -->
        <div class="flex flex-col items-center gap-4">
            <div class="h-12 w-[1px] bg-gradient-to-b from-slate-800 to-primary"></div>
            <p class="text-sm font-mono text-primary uppercase tracking-widest">There is a better way</p>
        </div>
    </div>
    
  </div>
</section>