---
import Button from './ui/Button.astro';
---

<section id="contact" class="py-24 bg-slate-950 relative overflow-hidden reveal border-t border-slate-900">
  <div class="container mx-auto px-6 md:px-12">
    <div class="bg-slate-900 rounded-3xl shadow-2xl overflow-hidden flex flex-col lg:flex-row border border-slate-800">
      
      <!-- Image Side -->
      <div class="lg:w-1/2 relative min-h-[400px]">
        <img 
          src="https://images.unsplash.com/photo-1586528116311-ad8dd3c8310d?auto=format&fit=crop&q=80" 
          alt="Canadian Metal Fabricators Automated Facility" 
          class="absolute inset-0 w-full h-full object-cover opacity-60"
        />
        <div class="absolute inset-0 bg-primary/90 mix-blend-multiply"></div>
        <div class="absolute inset-0 flex flex-col justify-center p-12 text-white relative z-10">
          <h3 class="font-montserrat font-bold text-3xl mb-4">Start Your Hybrid Journey</h3>
          <p class="text-blue-100 mb-8 max-w-md">
            Upload your STEP or DXF files. Get a quote that includes both domestic rapid-turn and global volume pricing.
          </p>
          <ul class="space-y-4 font-mono text-sm">
            <li class="flex items-center gap-3">
              <span class="bg-white/10 p-2 rounded backdrop-blur-sm">📍</span> 123 Manufacturing Blvd, GTA
            </li>
            <li class="flex items-center gap-3">
              <span class="bg-white/10 p-2 rounded backdrop-blur-sm">📧</span> quotes@cmfab.ca
            </li>
            <li class="flex items-center gap-3">
              <span class="bg-white/10 p-2 rounded backdrop-blur-sm">📞</span> +1 (416) 555-0123
            </li>
          </ul>
        </div>
      </div>

      <!-- Form Side -->
      <div class="lg:w-1/2 p-12 bg-slate-900">
        <form class="space-y-6">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div class="space-y-2">
              <label class="text-sm font-bold text-slate-300">First Name</label>
              <input type="text" class="w-full bg-slate-800 border border-slate-700 text-white rounded-lg px-4 py-3 focus:outline-none focus:border-primary transition placeholder-slate-500" placeholder="Jane" />
            </div>
            <div class="space-y-2">
              <label class="text-sm font-bold text-slate-300">Last Name</label>
              <input type="text" class="w-full bg-slate-800 border border-slate-700 text-white rounded-lg px-4 py-3 focus:outline-none focus:border-primary transition placeholder-slate-500" placeholder="Doe" />
            </div>
          </div>

          <div class="space-y-2">
            <label class="text-sm font-bold text-slate-300">Work Email</label>
            <input type="email" class="w-full bg-slate-800 border border-slate-700 text-white rounded-lg px-4 py-3 focus:outline-none focus:border-primary transition placeholder-slate-500" placeholder="jane@company.com" />
          </div>

          <div class="space-y-2">
            <label class="text-sm font-bold text-slate-300">Project Details</label>
            <textarea class="w-full bg-slate-800 border border-slate-700 text-white rounded-lg px-4 py-3 h-32 focus:outline-none focus:border-primary transition placeholder-slate-500" placeholder="Describe materials, tolerances, and estimated annual volume..."></textarea>
          </div>

          <button type="submit" class="w-full bg-primary text-white font-bold text-lg py-4 rounded-xl hover:bg-primary-dark transition shadow-lg shadow-primary/20">
            Submit RFQ
          </button>
          
          <p class="text-xs text-center text-slate-500 mt-4">
            NDA available upon request. Your IP is protected by ISO 27001 standards.
          </p>
        </form>
      </div>

    </div>
  </div>
</section>
