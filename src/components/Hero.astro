---
import Button from './ui/Button.astro';
import heroBg from '../assets/hero-service-bg.png';

---

<section class="relative w-full h-screen min-h-[600px] flex items-center justify-center overflow-hidden">
  <!-- Background Image -->
  <div class="absolute inset-0 z-0">
    <img 
      src={heroBg.src} 
      alt="Metal Fabrication Shop Floor" 
      class="w-full h-full object-cover"
    />
    <!-- Overlay -->
    <div class="absolute inset-0 bg-black/40"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 container mx-auto px-6 md:px-20 text-white">
    <div class="max-w-4xl space-y-8">
      <h1 class="font-montserrat font-bold text-5xl md:text-7xl leading-tight">
        Global Cost Structure. <br/>Domestic Security.
      </h1>
      
      <p class="font-poppins text-xl md:text-2xl font-normal opacity-90 max-w-2xl">
        The risk-insured manufacturing partner for Canadian OEMs. We combine the economics of global sourcing with the reliability of ISO 9001:2015 domestic fabrication.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 pt-4">
        <Button href="#quote" variant="primary">
          Calculate Your Savings
        </Button>
        <Button href="#capabilities" variant="outline">
          Explore Hybrid Model
        </Button>
      </div>
    </div>
  </div>
</section>
