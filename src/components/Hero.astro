---
import Button from './ui/Button.astro';
---

<section class="relative w-full min-h-[90vh] flex items-center overflow-hidden">
  <!-- Dynamic Background -->
  <div class="absolute inset-0 z-0">
    <img 
      src="https://images.unsplash.com/photo-1504917595217-d4dc5ebe6122?auto=format&fit=crop&q=80" 
      alt="High Precision Laser Cutting Sparks" 
      class="w-full h-full object-cover opacity-60"
    />
    <div class="absolute inset-0 bg-gradient-to-r from-slate-950 via-slate-950/90 to-transparent"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 container mx-auto px-6 md:px-12 pt-20 reveal">
    <div class="max-w-4xl">
      <!-- Badge -->
      <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-primary/30 bg-primary/10 backdrop-blur-sm mb-6">
        <span class="w-2 h-2 rounded-full bg-primary animate-pulse"></span>
        <span class="text-xs font-mono text-primary-light uppercase tracking-widest">CONSTRUCTION RISK ASSURANCE</span>
      </div>

      <h1 class="mb-6 drop-shadow-lg">
        Offshore Economics. <br/>
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-white">Domestic Guarantee.</span>
      </h1>
      
      <p class="text-xl md:text-2xl text-slate-300 font-light max-w-2xl mb-10 leading-relaxed border-l-4 border-primary pl-6">
        The only metal fabricator that backs low-cost global production with a local GTA factory. We manufacture for budget, but guarantee the schedule from Toronto.
      </p>

      <div class="flex flex-col sm:flex-row gap-4">
        <Button href="#quote" variant="primary" class="shadow-lg shadow-blue-500/20">
          Get a Risk-Free Estimate
        </Button>
        <Button href="#services" variant="outline">
          See Our Capacity
        </Button>
      </div>

      <!-- Trust Bar -->
      <div class="mt-20 pt-8 border-t border-white/10 flex flex-col md:flex-row gap-8 md:items-center text-slate-500 text-sm font-mono">
        <span>TRUSTED BY TIER 1 GC TEAMS & DEVELOPERS:</span>
        <div class="flex gap-8 opacity-50 grayscale hover:grayscale-0 transition-all duration-500">
            <!-- Mock Logos (Text for now, can be SVGs) -->
            <span class="font-bold text-lg tracking-tight hover:text-white">PCL</span>
            <span class="font-bold text-lg tracking-tight hover:text-white">ELLISDON</span>
            <span class="font-bold text-lg tracking-tight hover:text-white">MULTIPLEX</span>
            <span class="font-bold text-lg tracking-tight hover:text-white">BIRD</span>
        </div>
      </div>
    </div>
  </div>
</section>
